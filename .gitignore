# Local secret files
.env
*.tfvars
*.tfvars.json

# Terraform state (contains secrets in plain text!)
terraform.tfstate*
.terraform/

# Terraform lock file (contains provider version information)
.terraform.lock.hcl